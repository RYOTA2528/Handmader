$(function() {
  $('.slider').slick({
      arrows: false,  //ボタン非表示
      autoplay: true, //自動再生
      autoplaySpeed: 4000, //再生スピード
      arrows: true, //矢印の導入
      // dots: true, //ドットの導入
      // fade: true, //フェードの有効化
  });
  $('.slider1').slick({
      arrows: false,  //ボタン非表示
      autoplay: true, //自動再生
      autoplaySpeed: 4000, //再生スピード
      arrows: true, //矢印の導入
      // dots: true, //ドットの導入
      // fade: true, //フェードの有効化
  });
});